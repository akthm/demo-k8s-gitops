apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-incident-triage.fullname" . }}
  labels:
    {{- include "ai-incident-triage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ai-incident-triage.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
