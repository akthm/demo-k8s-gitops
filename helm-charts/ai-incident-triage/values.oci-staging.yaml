# OCI staging overrides for AI incident triage

config:
  LOKI_URL: ""
  ENABLE_LLM: "false"
  LLM_PROVIDER: "none"
  KUBE_DEFAULT_NAMESPACE: "platform"

resources:
  requests:
    cpu: 30m
    memory: 96Mi
  limits:
    cpu: 150m
    memory: 192Mi
