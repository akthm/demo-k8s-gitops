apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flask-app.fullname" . }}-config
  labels:
{{ include "flask-app.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{ toYaml . | indent 2 }}
{{- end }}
