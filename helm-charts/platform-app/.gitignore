# Ignore packaged local subcharts - they're built from source in this repo
# These are regenerated by `helm dependency update`
charts/flask-app-*.tgz
charts/nginx-front-*.tgz

# Keep external charts like redis from Bitnami
# !charts/redis-*.tgz

# Chart.lock can be ignored since local charts change frequently
Chart.lock
